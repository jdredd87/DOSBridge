# DOS Bridge — build 17

_Built 2026-09-01 from `C:\DosBridge`._

Generated by `makeinst.cmd` in the dev tree. **Nothing here is edited by hand**
— every file is a copy, and the next build overwrites the lot.

Two halves. You need both, and they are installed in this order:

## 1. `server\` — the Windows PC

Copy it somewhere permanent (e.g. `C:\dosbridge`), open a command prompt inside
it, and follow `INSTALL.txt`. In short:

    install.cmd            PATH + firewall (needs an admin prompt)
    check.cmd              verifies Python, Free Pascal, PATH, firewall
    dosd.cmd               the daemon — leave it running in its own window
    python selftest.py     proves the Windows half works with no hardware

Free Pascal is a separate download and is not bundled. You need the **i386
win32** compiler first, then the **i8086-msdos cross** package — the Win64
build is not a substitute.

## 2. `client\` — the DOS machine

Carry it over by floppy, USB, disk image or network share and run `INSTALL`
there. You cannot use the bridge to install the bridge.

`README.TXT` inside it lists the three things you must supply yourself: a
packet driver for your NIC, the mTCP binaries, and `CHOICE.COM`.

## Addresses

Check these before you build, and again on the DOS machine:

* `AI.BAT` — `SET SRV=` and `SET UPHOST=` must be **this Windows PC's LAN
  address**. If it was auto-detected it may have picked a VPN or virtual
  adapter; that produces a client which polls forever and is never answered.
* `MTCP.NEW` — the DOS machine's own static address. Nothing has checked that
  it is free on your network. Reserve it, or exclude it from the DHCP pool.

`MTCP.NEW` is deliberately **not** called `MTCP.CFG`. If you already have a
working config, merge the two by hand rather than letting an installer
overwrite something that took effort to get right.

## Compiler Options

FreePascal was used because it does A great job of doing DOS software and is still maintained.
So most of the .MD files here will point Claude to use FPC locally on the Windows machine.
This helps compile local on the Server to speed things up.
BUT you can tell Claude to use A compiler on the DOS machine itself.
Prompt such as **Lets created a project on the DOS machine in C:\NewGame and use the Borland Pascal 7 compiler located in C:\BP\BIN**
Doing something like this you could get Claude to compile software on the DOS machine.

## Future Changes

* Remove mTCP dependency
* Better documentation
* Install / Setup Video
* More Tools
* More Demos
* Better Claude .MD files to help new Claude sessions pick up the project and how it works.
* Add more cross compilers presetup/defined/ready? Open Watcom V2?
* Add more DOS compilers presetup/defined/ready? Turbo Pascal? Microsoft C? Borland C? Watcom C/C++?
